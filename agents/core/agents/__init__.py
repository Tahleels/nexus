"""Agent definitions package for the Hub Agents sub-project (agents/core/agents).

Currently empty — agent records are stored and loaded from SQL Server
(see blueprints/agents_hub_bp.py) rather than as Python classes here.
"""
