"""Memory package for the Hub Agents sub-project (agents/core/memory).

Currently empty — short-term conversation memory is implemented by the
ShortTermMemory class in agents/core/orchestrator/engine.py rather than here.
"""
